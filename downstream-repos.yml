# Downstream Repos — Projects bootstrapped with dev-kit
#
# When dev-kit is updated (push to main/master), the notify-downstream workflow
# opens an issue in each listed repo to notify them of available updates.
#
# To register a new project, add it to the list below.
# The setup script (scripts/setup.sh) will also offer to register automatically.
#
# Format: owner/repo

repos:
  # - ninanathalie/atlas
  # - ninanathalie/tempo
